const abc = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];
console.log(abc);

const abcLen = abc.length;

const lastCharPos = abcLen - 1;

const lastChar = abc[lastCharPos];

console.log(lastChar);

const abcAns = 'Is mums zinomos ' + abcLen + 'raidziu abeceles, paskutine yra: "' + lastChar + '".';

console.log(abcAns);
